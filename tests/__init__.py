"""Test suite for ras2fim-viewer-wasm.

Run everything the plain interpreter can:  python -m unittest discover tests
Run the browser reader's tests:            node --test tests/

The suite is split by what it needs, because the pipeline deliberately spans two
environments. See tests/README.md.
"""
